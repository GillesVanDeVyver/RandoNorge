# Launch instructions
#
# THIS PROJECT USES pnpm ONLY. Do not run `npm install` or `yarn` — a preinstall
# guard (only-allow pnpm) will block them. Mixing package managers is what caused
# the past CI failures (lockfile drift + phantom dependencies).

cd /home/gilles/Projects/PrivateProjects/RandoNorge/RandoNorge
pnpm install
pnpm dev

# `pnpm dev` starts both servers via concurrently:
#   - vite    -> frontend on http://localhost:5173
#   - wrangler -> worker/API on http://localhost:8787 (auth + D1)
#
# Run only one if needed:
#   pnpm dev:frontend   (vite only)
#   pnpm dev:worker     (wrangler only)

# Add / remove dependencies (this updates pnpm-lock.yaml automatically — commit it):
#   pnpm add <pkg>
#   pnpm remove <pkg>



#server side
pnpm install --lockfile-only
npx wrangler deploy
