# Launch instructions

cd /home/gilles/Projects/PrivateProjects/RandoNorge/RandoNorge
npm install
npm run dev

# `npm run dev` starts both servers via concurrently:
#   - vite    -> frontend on http://localhost:5173
#   - wrangler -> worker/API on http://localhost:8787 (auth + D1)
#
# Run only one if needed:
#   npm run dev:frontend   (vite only)
#   npm run dev:worker     (wrangler only)



#server side
pnpm install --lockfile-only
npx wrangler deploy

# Fix for CI ERR_PNPM_OUTDATED_LOCKFILE:
# If you add a package with npm, the pnpm lockfile drifts and CI (pnpm install --frozen-lockfile) fails.
# Regenerate and commit the pnpm lockfile after any dependency change:
#   pnpm install --lockfile-only
#   git add pnpm-lock.yaml && git commit -m "Update pnpm-lock.yaml" && git push
# Better: use one package manager. CI uses pnpm, so add deps with `pnpm add <pkg>` (not npm).
